import { HasAdminPermissionDirective } from './has-admin-permission.directive';

describe('HasAdminPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new HasAdminPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
